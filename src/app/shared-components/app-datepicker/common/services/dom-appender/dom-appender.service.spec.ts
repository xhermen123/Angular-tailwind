import {inject, TestBed} from '@angular/core/testing';
import { DomHelper } from './dom-appender.service';

xdescribe('Service: DomHelper', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DomHelper]
    });
  });

  it('should ...', () => {
    expect(DomHelper).toBeTruthy();
  });

  it('should check setYAxisPosition', () => {

  });
});
